import XCTest
@testable import Pulse

final class PulseSmokeTests: XCTestCase {
    func testAppCompilesAndRuns() {
        // Smoke test: verifies test target builds and links against Pulse module.
        // Real tests added in later tasks.
        XCTAssertTrue(true)
    }
}
